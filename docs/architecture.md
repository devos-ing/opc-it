# Architecture

Document the system's current components, public boundaries, dependency
direction, data flow, and operational constraints. Keep this file aligned with
the implemented system; decision history belongs in `docs/adr/`.
