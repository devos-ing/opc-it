# Architecture Decision Records

Create one numbered Markdown file per accepted decision only when the choice is
hard to reverse, surprising without context, and the result of a genuine
trade-off. Record status, context, decision, alternatives, and consequences.
Use `Proposed` until the related delivery plan is approved.
